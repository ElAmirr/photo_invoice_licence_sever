services:
  - type: web
    name: shootix-licence-server
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: shootix_db2
          property: connectionString
      - key: ADMIN_SECRET
        generateValue: true
      - key: PORT
        value: 3000
